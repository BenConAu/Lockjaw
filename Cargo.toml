[package]
name = "lockjaw"
version = "0.1.0"
edition = "2021"

[workspace]
members = ["xtask"]

[profile.dev]
panic = "abort"
opt-level = 1

[profile.release]
panic = "abort"
opt-level = 2
lto = true
